spring:
  application:
    name: oauth-service
  security:
    user:
      name: tk
      password: tk-123456
server:
  port: 8886
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
